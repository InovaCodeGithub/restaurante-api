const { WAConnection } = require('@adiwajshing/baileys/lib/WAConnection/0.Base')

const WWConnection = require('../connection')(HandleConnection)

module.exports = clientChat = async () => {
  await WWConnection
}
